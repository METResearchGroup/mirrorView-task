[project]
name = "mirrorview-task"
version = "0.1.0"
description = "jsPsych mirror view experiment — tooling and static assets"
readme = "README.md"
requires-python = ">=3.12,<3.13"

[dependency-groups]
dev = [
    "boto3>=1.42.88",
    "pandas>=3.0.2",
    "questionary>=2.1.1",
    "typer>=0.25.0",
]

[tool.uv]
package = false
