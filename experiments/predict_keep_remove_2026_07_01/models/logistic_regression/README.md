# Training a logistic regression model

Various versions of training logistic regression models on post embeddings. Specifically, we train on the original post embedding vs. training on the difference between the original embedding and the mirrored embedding.
