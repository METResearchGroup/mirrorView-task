# Training an XGBoost model

Various versions of training XGBoost models on post embeddings. Specifically, we train on the original post embedding vs. training on the difference between the original embedding and the mirrored embedding.
